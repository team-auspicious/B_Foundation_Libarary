name: testing npm publish

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string

jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v4
      with:
        publish-version: ${{ inputs.version }}
    - run: echo "got ${{ publish-version }}"
